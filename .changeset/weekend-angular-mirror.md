---
"@verdocs/angular-sdk": minor
---

Mirrors the React catalog's 21 new controls as native Angular standalone components with signal inputs/outputs and model() two-way bindings (VerdocsCheckbox through VerdocsOrganizationCard). TemplateRef-based cell and icon slots where React used ReactNode props. The compiled stylesheet gained the same per-signer color tokens and classes as the React SDK.
