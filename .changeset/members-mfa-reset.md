---
'@verdocs/js-sdk': minor
---

Add `resetOrganizationMemberMFA` for org admins and owners. Append `mfa_enabled` / `mfa_enrolled_at` on user records returned in member lists, if the caller is an admin or owner.
