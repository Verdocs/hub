{
  "name": "@verdocs/hub",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:report": "vitest run",
    "conformance": "pnpm --if-present --no-bail -r run conformance",
    "generate:docs": "node scripts/clean-sdk-docs.mjs && turbo run generate-sdk-docs --filter=!@verdocs/js-sdk && pnpm --filter @verdocs/js-sdk docs",
    "changeset:version": "changeset version",
    "changeset:add": "changeset",
    "changeset:status": "changeset status",
    "changeset:publish": "changeset publish",
    "changeset:docs": "node ./scripts/generate-changelog.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.3",
    "@vitest/ui": "^5.0.1",
    "turbo": "^2.11.0",
    "typescript": "6.0.3",
    "vitest": "^5.0.1"
  },
  "packageManager": "pnpm@10.34.4",
  "engines": {
    "node": ">=24"
  }
}