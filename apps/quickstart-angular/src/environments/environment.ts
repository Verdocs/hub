/**
 * The Angular equivalent of the React quickstart's VITE_VERDOCS_API_BASE
 * override: change the value here, or add an angular.json fileReplacement
 * for per-environment builds.
 */
export const environment = {
  apiBase: 'https://stage-api.verdocs.com',
};
