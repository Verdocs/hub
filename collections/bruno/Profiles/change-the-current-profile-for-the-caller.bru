meta {
  name: Change the "current" profile for the caller
  type: http
  seq: 1
}

post {
  url: {{base_url}}/v2/profiles/:profile_id/switch
  body: none
  auth: inherit
}

params:path {
  profile_id:
}
