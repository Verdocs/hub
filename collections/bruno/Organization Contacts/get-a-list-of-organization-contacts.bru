meta {
  name: Get a list of organization contacts
  type: http
  seq: 2
}

get {
  url: {{base_url}}/v2/organization-contacts
  body: json
  auth: inherit
}

body:json {
  {
    "email": "string",
    "token": "string"
  }
}
