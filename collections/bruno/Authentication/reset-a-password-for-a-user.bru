meta {
  name: Reset a password for a user
  type: http
  seq: 6
}

post {
  url: {{base_url}}/v2/users/reset-password
  body: json
  auth: inherit
}

body:json {
  {
    "email": "string",
    "code": "string",
    "new_password": "string"
  }
}
