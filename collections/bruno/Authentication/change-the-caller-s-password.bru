meta {
  name: Change the caller's password
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v2/users/change-password
  body: json
  auth: inherit
}

body:json {
  {
    "old_password": "string",
    "new_password": "string"
  }
}
