meta {
  name: Create a template from an asset in Sharepoint
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v2/templates/from-sharepoint
  body: json
  auth: inherit
}

body:json {
  {
    "name": "string",
    "siteId": "string",
    "itemId": "string",
    "oboToken": "string"
  }
}
