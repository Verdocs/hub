meta {
  name: Create brand
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v2/organizations/:organizationid/brands
  body: json
  auth: inherit
}

params:path {
  organizationid:
}

body:json {
  {
    "key": "string",
    "timezone": "string",
    "locale": "string"
  }
}
