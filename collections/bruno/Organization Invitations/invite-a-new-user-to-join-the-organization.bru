meta {
  name: Invite a new user to join the organization
  type: http
  seq: 5
}

post {
  url: {{base_url}}/v2/organization-invitations
  body: json
  auth: inherit
}

body:json {
  {
    "email": "string",
    "first_name": "string",
    "last_name": "string",
    "role": "contact"
  }
}
